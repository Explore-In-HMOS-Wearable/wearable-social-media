import { ILocalMedia } from '../../model/Interface/ILocalMedia';
import { MediaType } from '../../model/LocalMedia';

export const mockLocalMediaDataList : Array<ILocalMedia> = [
  {
    id: 1,
    imageUrl: 'string',
    type: MediaType.Recent
  }
]