import { IProfileData } from '../../model/Interface/IPRofileData';
import { mockUserData } from './MockUserData';

export const mockProfileData : IProfileData = {
  id: 1,
  biography: 'string',
  lastSeen: 'string',
  user: mockUserData,
  photo: 'string',
}